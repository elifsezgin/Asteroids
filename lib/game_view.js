function GameView(game, ctx) {
  this.game = game;
  this.ctx = ctx;
}

GameView.prototype.start = function() {
  setInterval(Game.prototype.moveObjects {
    setInterval(Game.prototype.draw)
  }, 20);
};
